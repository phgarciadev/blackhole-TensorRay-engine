# UX UI

Camada de Aplicação do Usuário.
Contém o código específico do "Black Hole Simulator" (telas, menus, controles).
Compila na biblioteca estática `libbhs_ui.a`.
Depende do gui-framework (`ux/lib`) para desenhar e da simulação (`sim`) para dados.
